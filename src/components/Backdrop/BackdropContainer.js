import BackdropComponent from './BackdropComponent';

// @TODO jogo para adivinhar de qual filme é a capa.

const BackdropContainer = ({ ...props }) => <BackdropComponent {...props} />;

export default BackdropContainer;
